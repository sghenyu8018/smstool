# 环境变量配置示例
# 复制此文件为 .env 并填写实际值

# ========== SSO登录凭据（必需） ==========
# 用于阿里巴巴内部系统SSO登录
SSO_USERNAME=your_username
SSO_PASSWORD=your_password

# ========== 会话配置（可选） ==========
# 会话文件名，默认为 aliyun_session.json
# SESSION_FILE=aliyun_session.json

# Playwright storage_state 文件名，默认为 storage_state.json
# STORAGE_STATE_FILE=storage_state.json

# ========== 浏览器配置（可选） ==========
# 是否无头模式运行，默认为 False（显示浏览器窗口）
# HEADLESS=False

# 浏览器类型，可选值：chromium, firefox, webkit，默认为 chromium
# BROWSER_TYPE=chromium

# 浏览器渠道，可选值：chrome, msedge, chrome-beta, chrome-dev 等
# 如果未设置，则使用 Playwright 自带的浏览器
# BROWSER_CHANNEL=msedge

# 浏览器操作超时时间（毫秒），默认为 60000（60秒）
# BROWSER_TIMEOUT=60000

# ========== 日志配置（可选） ==========
# 日志级别，可选值：DEBUG, INFO, WARNING, ERROR，默认为 INFO
# LOG_LEVEL=INFO

# ========== DashScope API配置（可选） ==========
# 如果使用 browser-use 库，需要配置此选项
# DASHSCOPE_API_KEY=your_dashscope_api_key
# DASHSCOPE_MODEL=qwen-vl-max-latest

# ========== 其他配置（可选） ==========
# 根据项目需要添加其他配置项